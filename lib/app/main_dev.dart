// Flavor entry point: Development
// Run with: flutter run -t lib/app/main_dev.dart
//
// Uncomment and configure when you need flavor-specific behavior
// (e.g. different API base URL, debug overlays, test accounts).

// import 'bootstrap.dart';
// void main() => bootstrap(environment: AppEnvironment.dev);
