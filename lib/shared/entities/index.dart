// Shared entities barrel — import individual files or this barrel.
//
// Usage:
//   import 'package:harvest_app/shared/entities/address.dart';
//   import 'package:harvest_app/shared/entities/user_profile.dart';
//
// Note: PaginatedResponse is in core/models/ — it is infrastructure-level
// and can stay there. Add it here if a feature finds core/ too low-level.

export 'address.dart';
export 'user_profile.dart';
